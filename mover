sudo cp ssh/ssh_config /etc/ssh/ssh_config
sudo cp ssh/sshd_config /etc/ssh/sshd_config

sudo touch usr/share/X11/xorg.conf.d/20-thinkpad.conf
sudo cp 20-thinkpad.conf /usr/share/X11/xorg.conf.d/20-thinkpad.conf

sudo cp zprofile /etc/zsh/zprofile
sudo cp pacman.conf /etc/pacman.conf

sudo cp lock /bin/lock
sudo chmod +x /bin/lock

cp .vimrc ~/.vimrc
cp -r .vim ~/.vim
cp .xinitrc ~/.xinitrc
cp .Xdefaults ~/.Xdefaults
cp .zshrc ~/.zshrc
cp .gitconfig ~/.gitconfig
cp .blockify_list ~/.blockify_list

cp tmux.conf ~/.tmux.conf

mkdir ~/.config
mkdir ~/.config/bspwm
mkdir ~/.config/sxhkd

cp bspwmrc ~/.config/bspwm/
cp sxhkdrc ~/.config/sxhkd/

rm -rf ~/.weechat
cp weechat ~/.weechat

mkdir ~/Documents
mkdir ~/Builds
mkdir ~/Downloads
